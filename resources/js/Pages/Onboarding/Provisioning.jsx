import { Head, useForm, Link, router } from '@inertiajs/react';

export default function Provisioning() {
    return (
        <div className="p-6">
            <h1 className="text-2xl font-bold mb-4">Provisioning Tenant</h1>
            <p>Please wait while we set up your tenant environment...</p>
        </div>
    );
}
